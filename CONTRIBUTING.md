# 协作指南

本项目由两名开发者协作维护，`main` 必须始终保持可构建。

## 分支

- 功能：`feature/...`
- 修复：`fix/...`
- 工程：`chore/...`

不要直接在 `main` 开发。每个 Pull Request 由另一名开发者检查后合并。

## 提交前

1. 更新适用计划、文档、测试与迁移。
2. 运行 `.\scripts\check.ps1`。
3. 分别记录 Windows 与 Android 验证结果；未运行必须说明原因。
4. 检查 API、迁移和安全影响。
5. 检查 diff，确保没有密钥、真实员工数据或调试输出。

不允许通过删除测试、跳过失败检查或降低安全设置来使 CI 变绿。

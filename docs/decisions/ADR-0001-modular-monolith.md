# ADR-0001：采用 Django 模块化单体

- 状态：已接受
- 日期：2026-08-14

## 背景

项目由两名开发者协作，当前业务边界仍在形成，只有健康检查需要真实实现。

## 决定

采用单一 Django 部署单元，并把账号、组织、员工、审计等边界放入独立 Django app。共享数据库与部署，但禁止跨模块随意耦合。

## 结果

- 本地开发、测试、迁移与部署更简单。
- 可以在业务稳定后通过清晰模块边界评估是否需要拆分。
- 当前不引入服务发现、分布式事务、消息总线或微服务运维成本。
